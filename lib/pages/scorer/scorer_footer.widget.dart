import 'package:flutter/material.dart';

class ScorerFooter extends StatelessWidget {
  const ScorerFooter({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
